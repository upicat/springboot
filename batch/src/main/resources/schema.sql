
create table PERSON
(
  id          NUMBER not null auto_increment primary key,
  name        VARCHAR2(20),
  age         NUMBER,
  nation      VARCHAR2(20),
  address     VARCHAR2(20)
);

